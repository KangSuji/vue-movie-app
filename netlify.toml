[build]
  command = "npm run build"
  functions = "functions"
  publish = "dist"

[dev]
  framwork = "#custom"
  command = "npm run dev"
  targetPort = 8090
  port = 8888
  publish = "dist"
  autoLanuch = false
